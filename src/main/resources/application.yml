server:
  port: 8888
  servlet:
    context-path: /iMana/api

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/iMana
    username: temp_user
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

debug: true
